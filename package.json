{
    "name": "i-analyzer",
    "version": "1.4.3",
    "license": "MIT",
    "scripts": {
        "postinstall": "yarn install-backend && yarn install-frontend",
        "install-backend": "cd backend && pip install -r requirements.txt",
        "install-frontend": "cd frontend && yarn",
        "test-once": "cd frontend && ng test --watch=false"
    },
    "private": true,
    "devDependencies": {},
    "dependencies": {}
}
