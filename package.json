{
    "name": "i-analyzer",
    "version": "2.4.0",
    "license": "MIT",
    "scripts": {
        "postinstall": "yarn install-backend && yarn install-frontend",
        "install-backend": "cd backend && pip install -r requirements.txt --no-deps --ignore-installed",
        "install-frontend": "cd frontend && npm install",
        "build-frontend": "cd frontend && npm run build",
        "test-once": "cd frontend && ng test --watch=false"
    },
    "private": true,
    "devDependencies": {},
    "dependencies": {}
}
